<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sterrenwacht.cozmix">

    <application
        android:name=".helper.GlobalVariables"
        android:allowBackup="false"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AstronautsActivity"
            android:label="@string/manage_astronauts_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".main.PlanetenpadInnerFragment" />
        <activity android:name=".planetenpad.PlanetActivity" />
    </application>

</manifest>